[package]
name = "scarllet-sdk"
version = "0.1.0"
edition = "2021"

[dependencies]
scarllet-proto = { path = "../scarllet-proto" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
dirs = "6"
tracing = "0.1"

[target.'cfg(unix)'.dependencies]
libc = "0.2"
